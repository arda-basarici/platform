# Managed by ansible (roles/hardening). Key-only ssh, no root login. A drop-in,
# never an edit of sshd_config itself; `sshd -T` shows the effective result.
PasswordAuthentication no
PermitRootLogin no
KbdInteractiveAuthentication no
